# Benchmarks

Keep benchmark code separate from runtime. Measure ASR partial latency, ASR RTF, TTS first-audio latency, TTS RTF, and optional end-to-end turn latency. Store generated results under `benchmark/results/`.
